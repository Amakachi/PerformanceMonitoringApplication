import { NewRoleDirective } from './new-role.directive';

describe('NewRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new NewRoleDirective();
    expect(directive).toBeTruthy();
  });
});
